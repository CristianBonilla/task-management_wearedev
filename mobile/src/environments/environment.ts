export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8000/api',
  expiringWindowHours: 48, // Window (in hours) used to flag tasks as "expiring soon". Must match backend.
};
